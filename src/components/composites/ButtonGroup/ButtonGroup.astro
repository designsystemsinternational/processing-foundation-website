---
import styles from './ButtonGroup.module.css';

interface Props {
  hasAccent?: boolean;
}

const { hasAccent = false } = Astro.props;
---

<div class={styles.buttonGroup} data-has-accent={hasAccent}>
  <slot />
</div>
