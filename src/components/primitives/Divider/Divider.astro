---
import cn from 'clsx';
import {
  blockDefaults,
  type DividerSize,
  type ThreadSpan,
} from '@/lib/constants.ts';
import styles from './Divider.module.css';

export interface Props {
  threadSpan?: ThreadSpan;
  size?: DividerSize;
}

const {
  threadSpan = blockDefaults.threadSpan,
  size = blockDefaults.dividerSize,
} = Astro.props;

const numThreads = 12 / threadSpan;
---

<div class={cn('layout-grid', styles.root)} data-size={size}>
  <div class="row">
    {[...Array(numThreads)].map(() => <div class={styles.thread} />)}
  </div>
</div>
