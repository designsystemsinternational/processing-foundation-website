---
import styles from "./ImagePlaceholder.module.css";

interface Props {
  patterned?: boolean;
}

const { patterned = true }: Props = Astro.props;
---

<div class={styles.root} data-patterned={patterned} />
