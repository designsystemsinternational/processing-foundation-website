---
import type { block3 } from "@/schemas/pages.ts";
import type { z } from "zod";

type Props = z.infer<typeof block3>;
const { title, description } = Astro.props;
---

<section data-block="block3">
  <h2>{title}</h2>
  <p>{description}</p>
</section>
