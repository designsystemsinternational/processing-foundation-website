---
import cn from 'clsx';
import Image from '@/components/Image';
import Block, { type Props as BlockProps } from '@/components/Block';
import type { Images } from '@/schemas/pages.ts';
import styles from './Images.module.css';

export type Props = Images & BlockProps;

const { images, variant = 'full', ...block } = Astro.props;
---

<Block {...block}>
  <div class={cn('row', styles.root)} data-variant={variant}>
    {images.map((item) => <Image {...item} />)}
  </div>
</Block>
