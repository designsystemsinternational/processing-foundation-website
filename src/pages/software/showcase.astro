---
import { getCollection } from "astro:content";
import ShowcaseLayout from "@/components/layouts/ShowcaseLayout";

const channels = await getCollection("showcase");
---

<ShowcaseLayout channels={channels} />
